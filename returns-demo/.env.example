# 复制为 .env 后修改；.env 不要提交到仓库
# MySQL root 密码（演示中应用直连 root，两者保持一致）
MYSQL_ROOT_PASSWORD=change-me-root
DB_PASSWORD=change-me-root
DB_USER=root

# 数据库名与应用对外端口
DB_NAME=returns_demo
APP_PORT=3000

# 前后端分离部署时填写前端来源（逗号分隔）；同源部署留空
CORS_ALLOWED_ORIGINS=
